<?php

namespace Workbench\App\Ship\Helpers;

class ExplosiveClass
{
    // This class is used to test if we can handle requiring helper files from
    // a directory where it has a class (which is already declared) in it.
}
