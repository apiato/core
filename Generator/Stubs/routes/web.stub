<?php

use App\{{section-name}}\{{container-name}}\UI\WEB\Controllers\Controller;
use Illuminate\Support\Facades\Route;

Route::{{http-verb}}('{{endpoint-url}}', [Controller::class, '{{operation}}'])
    ->name('{{route-name}}')
    ->middleware(['auth:{{auth-middleware}}']);

