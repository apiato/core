<?php

namespace App\Containers\{{section-name}}\{{container-name}}\Tests\Unit;

use App\Containers\{{section-name}}\{{container-name}}\Tests\TestCase;

/**
 * Class {{class-name}}.
 *
 * @group {{_container-name}}
 * @group unit
 */
class {{class-name}} extends TestCase
{
    public function test(): void
    {
        // create a data object
        $data = [
            // 'key' => 'value',
        ];

        /**
         * you may want to do something like:
         *
         * 1) create a new Request with this data
         * 2) create a specific Action or Task
         * 3) pass the Request to the Action / Task
         * 4) assert that everything is correct!
         *
         */

        // assert something here
        self::assertEquals(true, true);
    }
}
